# 🚀 Деплой Telegram бота

## Для онлайн хостингу

### Файли які потрібні:

```
bot.py              # Основний файл бота
requirements.txt    # Залежності
```

### Крок 1: Завантажте файли

Завантажте ці файли на ваш хостинг.

### Крок 2: Встановіть залежності

```bash
pip install -r requirements.txt
```

або

```bash
pip3 install python-telegram-bot==20.7
```

### Крок 3: Запустіть бота

```bash
python3 bot.py
```

або

```bash
nohup python3 bot.py > bot.log 2>&1 &
```

### Крок 4: Перевірте що бот працює

```bash
python3 test_bot.py
```

Має показати:
```
✅ Бот працює!
   Ім'я: TimeKeeper Backup Bot
   Username: @backuptimekeeper_bot
```

## Як працює авторизація

1. **Користувач відкриває бота** в Telegram
2. **Натискає /start**
3. **Бот генерує код** (наприклад: 123456)
4. **Користувач вводить код** в додатку TimeKeeper
5. **Додаток перевіряє код** через getUpdates API
6. **Знаходить повідомлення з кодом** → отримує chat_id
7. **Зберігає chat_id** → авторизація завершена!

## Важливо!

- Бот має бути **постійно запущений**
- Коди зберігаються **в пам'яті** (не в файлах)
- Коди дійсні **5 хвилин**
- Після рестарту бота **старі коди видаляються**

## Логи

Бот виводить логи в консоль:

```
✅ Code 123456 for John (chat_id: 123456789)
📊 Active codes: 1
```

## Troubleshooting

### Бот не запускається

```bash
# Перевірте токен
python3 test_bot.py

# Перевірте залежності
pip3 list | grep telegram
```

### Код не знаходиться

1. Перевірте що бот запущений
2. Перевірте що користувач натиснув /start
3. Перевірте логи бота
4. Спробуйте новий код

### Бот не відповідає

```bash
# Перевірте процес
ps aux | grep bot.py

# Перезапустіть
pkill -f bot.py
python3 bot.py
```

## Альтернативні хостинги

### Heroku

```bash
# Додайте Procfile:
web: python3 bot.py
```

### Railway

```bash
# Додайте railway.json:
{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python3 bot.py"
  }
}
```

### PythonAnywhere

1. Upload files
2. Open Bash console
3. Run: `python3 bot.py`

---

**Готово!** Бот працює 24/7 🎉
